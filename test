a!localVariables(
  local!cleanStates: reject(
    fn!or(
      fn!isnull(_),
      fn!isblank(_)
    ),
    pv!residenceStates
  ),
  a!dropdownField(
    label: "Residence State",
    choiceLabels: local!cleanStates,
    choiceValues: local!cleanStates,
    value: ri!selectedState,
    saveInto: ri!selectedState
  )
)
