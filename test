a!textDisplayField(
  label: "Debug - Residence States",
  value: if(
    isnull(local!residenceStates),
    "residenceStates is null",
    "Count: " & length(local!residenceStates) & " - Values: " & joinarray(local!residenceStates, ", ")
  )
)
